psutil>=5.9.0
watchdog>=3.0.0
plyer>=2.1.0

# Optional / platform-specific (uncomment what you need)
# pywin32>=306          # Windows only - required for webcam registry checks
# opencv-python>=4.8.0  # optional - webcam "busy" fallback check on macOS/Linux
# GPUtil>=1.4.0         # optional - GPU encoder load heuristic (NVIDIA only)
# requests>=2.31.0      # optional - Slack/Teams webhook alerts
